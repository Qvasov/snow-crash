1. strings level07
2. Видим что используется переменная LOGNAME а следом видим что данные подставляются в командуecho
3. Меняем переменную LOGNAME на $(getflag) export LOGNAME=$\(getflag\)
4. Запускаем файл видим пароль
